bin_PROGRAMS = ussp-push

# set the include path found by configure
AM_CFLAGS = $(OPENOBEX_CFLAGS) $(BLUETOOTH_CFLAGS)

# the library search path.
LIBS = $(OPENOBEX_LIBS) $(BLUETOOTH_LIBS)

ussp_push_SOURCES = obex_main.c obex_sdp.c obex_socket.c obex_macros.h obex_main.h obex_sdp.h obex_socket.h
